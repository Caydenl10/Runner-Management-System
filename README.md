**Runner Management System**

A JavaFX desktop application for managing race runner records with persistent storage in a MySQL database. The system allows users to create, view, update, and delete runner entries through an interactive graphical interface while maintaining a clear separation between the user interface, controller logic, and database access layer. Database operations are handled using JDBC prepared statements with auto-generated primary keys to ensure secure and reliable data management.

